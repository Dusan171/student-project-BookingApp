<UserControl x:Class="BookingApp.Presentation.Owner.ReviewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookingApp.Presentation.Owner"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#f2f4f8">

    <Grid>
        <Grid Margin="20,200,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Margin="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="10">
                <StackPanel>
                    <TextBlock Text="Host → Guest"
                    FontSize="20"
                    FontWeight="Bold"
                    Margin="0,20,0,10"
                    HorizontalAlignment="Center"/>
                    <DataGrid x:Name="HostToGuestGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  RowBackground="White"
                  AlternatingRowBackground="#f2f4f8">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Guest Name" Binding="{Binding ReservationId}" Width="*" />
                            <DataGridTextColumn Header="Cleanliness" Binding="{Binding CleanlinessRating}" Width="Auto" />
                            <DataGridTextColumn Header="Rule Respecting" Binding="{Binding RuleRespectingRating}" Width="Auto" />
                            <DataGridTextColumn Header="Comment" Binding="{Binding Comment}" Width="2*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>


            <Border Grid.Column="1" Margin="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="10">
                <StackPanel>
                    <TextBlock Text="Guest → Host"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,20,0,10"
                   HorizontalAlignment="Center"/>
                    <DataGrid x:Name="GuestToHostGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  RowBackground="White"
                  AlternatingRowBackground="#f2f4f8">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Guest Name" Binding="{Binding ReservationId}" Width="*" />
                            <DataGridTextColumn Header="Cleanliness" Binding="{Binding CleanlinessRating}" Width="Auto" />
                            <DataGridTextColumn Header="Owner's Reliability" Binding="{Binding OwnerRating}" Width="Auto" />
                            <DataGridTextColumn Header="Comment" Binding="{Binding Comment}" Width="2*" />
                            <DataGridTemplateColumn Header="Images" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Click="ImagesButton_Click" Padding="0" Background="Transparent" BorderThickness="0">
                                                <StackPanel Orientation="Horizontal">
                                                    <Image Source="/Resources/Images/photos.png" Width="16" Height="16" Margin="0,0,5,0"/>
                                                    <TextBlock Text="{Binding ImageCount, StringFormat=+{0}}" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
        <local:ImageGalleryView x:Name="ImageGallery" Visibility="Collapsed" />
    </Grid>
</UserControl>
