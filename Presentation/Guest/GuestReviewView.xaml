<Window x:Class="BookingApp.Presentation.Guest.GuestReviewView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GuestReviewView" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Cleanliness Rating (1-5):" FontWeight="Bold"/>
            <TextBox x:Name="CleanlinessTextBox" Width="50"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="1" Margin="0,0,0,10">
        <TextBlock  Text="Owner Rating (1-5):" FontWeight="Bold"/>
        <TextBox x:Name="OwnerTextBox" Width="50"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Comment:" FontWeight="Bold"/>
            <TextBox x:Name="CommentTextBox" Height="60" AcceptsReturn="True" TextWrapping="Wrap"/>
        </StackPanel >

        <StackPanel Orientation="Vertical" Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="Image Paths (separeted by ;):" FontWeight="Bold"/>
            <TextBox x:Name="ImagesTextBox" Height="60" AcceptsReturn="True" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="4" Margin="0,10,0,0">
            <Button Content="Submit" Width="100" Margin="5" Click="Submit_Click"/>
            <Button Content="Cancel" Width="100" Margin="5" Click="Cancel_Click"/>
        </StackPanel>

    </Grid>
</Window>
