<Window x:Class="BookingApp.Presentation.Guest.AccommodationReservationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AccommodationReservationView" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="CalendarDayButton">
            <Style.Triggers>
                <Trigger Property="IsBlackedOut" Value="True">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="DarkRed"/>
                    <Setter Property="ToolTip" Value="Occupied"/>
                    </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Select stay period:" FontWeight="Bold" Margin="0,0,0,5"/>
            
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            
           <StackPanel Margin="0,0,20,0">
             <TextBlock Text="Start Date"/>
             <DatePicker x:Name="StartDatePicker"/>
           </StackPanel>
            
          <StackPanel>
             <TextBlock Text="End Date"/>
             <DatePicker x:Name="EndDatePicker"/>
          </StackPanel>
            
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Number of guests:"/>
            <TextBox x:Name="GuestsTextBox" Width="100"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Reserve" Width="100" Height="30" HorizontalAlignment="Right" 
                Click="Reserve_Click"/>
        </Grid>
</Window>
