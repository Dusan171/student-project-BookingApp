<UserControl x:Class="BookingApp.Presentation.Owner.ImageGalleryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookingApp.Presentation.Owner"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#80000000" Focusable="True"
             KeyDown="UserControl_KeyDown">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="600" Height="400" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <Button Content="X" HorizontalAlignment="Right" Margin="10"  Command="{Binding CloseCommand}" FontWeight="Bold" FontSize="16" Background="Transparent"/>

        <Image x:Name="DisplayedImage" Grid.Row="1" Stretch="Uniform" Source="{Binding CurrentImage}" Margin="10" />

        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="⟨" Width="40" Height="30" Margin="5" Command="{Binding PrevCommand}" Background="Transparent"/>
            <Button Content="⟩" Width="40" Height="30" Margin="5" Command="{Binding NextCommand}" Background="Transparent"/>
        </StackPanel>
    </Grid>
</UserControl>
