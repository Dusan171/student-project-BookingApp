<UserControl x:Class="BookingApp.Presentation.View.Owner.UnratedGuestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border BorderBrush="#C5D5E4" BorderThickness="1" CornerRadius="10" Background="#F8FAFC" Padding="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel HorizontalAlignment="Stretch">
                    <ItemsControl ItemsSource="{Binding UnratedReservations}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#C5D5E4" BorderThickness="1" CornerRadius="8" Background="White" Margin="0,0,0,10" Padding="15">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0"
                                                   Text="Unrated Guest"
                                                   FontSize="28"
                                                   FontWeight="SemiBold"
                                                   Foreground="#2D3E50"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,0,0,10"/>

                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <Grid Grid.Column="0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Guest name:" FontSize="14" Foreground="#555" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Id}" FontSize="16" FontWeight="SemiBold" Foreground="#2D3E50" VerticalAlignment="Center"/>
                                            </Grid>

                                            <Grid Grid.Column="1" HorizontalAlignment="Right">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Check in date:" FontSize="14" Foreground="#555"/>
                                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding StartDate, StringFormat='{}{0:dd.MM.yyyy.}'}" FontSize="14" FontWeight="SemiBold" Foreground="#2D3E50" Margin="5,0,0,0"/>

                                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Check out date:" FontSize="14" Foreground="#555" Margin="0,5,0,0"/>
                                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding EndDate, StringFormat='{}{0:dd.MM.yyyy.}'}" FontSize="14" FontWeight="SemiBold" Foreground="#2D3E50" Margin="5,5,0,0"/>
                                            </Grid>
                                        </Grid>

                                        <Button Grid.Row="2"
                                                Content="Rate now"
                                                Width="120"
                                                Height="35"
                                                HorizontalAlignment="Center"
                                                Background="#2D3E50"
                                                Foreground="White"
                                                FontWeight="SemiBold"
                                                BorderThickness="0"
                                                Margin="0,15,0,0"
                                                Command="{Binding DataContext.RateGuestCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                                CommandParameter="{Binding Id}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="8"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>