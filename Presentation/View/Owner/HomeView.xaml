<UserControl x:Class="BookingApp.Presentation.View.Owner.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookingApp.Presentation.View.Owner"
             mc:Ignorable="d" d:DesignHeight="720" d:DesignWidth="1200">
    <Grid Margin="0,200,0,100">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.7*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#F2F2F2" CornerRadius="30" Margin="10,10,10,76" Padding="20">
            <StackPanel HorizontalAlignment="Center" Width="487">
                <Ellipse Width="100" Height="100" Stroke="Gray" StrokeThickness="2" Margin="0,0,0,10">
                    <Ellipse.Fill>
                        <SolidColorBrush Color="LightCyan"/>
                    </Ellipse.Fill>
                </Ellipse>

                <TextBlock Text="{Binding Username}" x:Name="UsernameTextBlock" FontWeight="Bold" FontSize="16" Margin="0,0,0,20" TextAlignment="Center"/>

                <Button x:Name="UnratedButton" Content="🔔 Unrated" Margin="0,5" Command="{Binding ShowUnratedGuestsCommand}" />
                <Button Content="📚 My units" Margin="0,5" Command="{Binding ShowMyUnitsCommand}"/>
                <Button Content="💬 Forums" Margin="0,5" Command="{Binding ShowForumsCommand}"/>
            </StackPanel>
        </Border>

        <ContentControl Content="{Binding CurrentContent}" x:Name="MainContent" Grid.Column="1" Margin="20"/>
    </Grid>
</UserControl>