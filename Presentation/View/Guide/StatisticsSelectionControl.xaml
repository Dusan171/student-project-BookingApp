<Page x:Class="BookingApp.Presentation.View.Guide.StatisticsSelectionControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:BookingApp.Utilities"
      xmlns:controls="clr-namespace:BookingApp.Presentation.View.Guide.Controls"
      Background="White">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/StatisticsSelectionStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
            <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Margin="20">
        <TabControl>
            <TabItem Header="Tour Statistics">
                <controls:TourStatisticsControl DataContext="{Binding}"/>
            </TabItem>

            <TabItem Header="Request Statistics">
                <controls:RequestStatisticsControl DataContext="{Binding}"/>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
