<Page x:Class="BookingApp.Presentation.View.Guide.ReviewsControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:BookingApp.Utilities"
      xmlns:controls="clr-namespace:BookingApp.Presentation.View.Guide.Controls"
      Title="ReviewsPage"
      Background="#F5F5F5">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/ReviewsControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <local:StarConverter x:Key="StarConverter"/>
            <local:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <!-- Main Content -->
        <StackPanel Margin="16">
            <TextBlock Text="Reviews" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="540">
                <ItemsControl ItemsSource="{Binding ReviewDisplays}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <controls:ReviewCardControl DataContext="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>

        <!-- Image Overlay -->
        <controls:ImageOverlayControl DataContext="{Binding}"/>
    </Grid>
</Page>
