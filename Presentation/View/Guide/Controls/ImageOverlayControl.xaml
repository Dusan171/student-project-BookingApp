<UserControl x:Class="BookingApp.Presentation.View.Guide.Controls.ImageOverlayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BookingApp.Utilities">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/ReviewsControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <local:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="#CC000000" Visibility="{Binding IsOverlayVisible, Converter={StaticResource BoolToVisibilityConverter}}" Panel.ZIndex="1000">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>

            <!-- Left Navigation -->
            <Button Content="?" Grid.Column="0" Command="{Binding PrevImageCommand}" Style="{StaticResource NavButtonStyle}" ToolTip="Previous image"/>

            <!-- Main Image -->
            <Image Source="{Binding LargeImage}" Stretch="Uniform" Grid.Column="1"/>

            <!-- Right Navigation -->
            <Button Content="?" Grid.Column="2" Command="{Binding NextImageCommand}" Style="{StaticResource NavButtonStyle}" ToolTip="Next image"/>

            <!-- Close Button -->
            <Button Content="X" FontSize="16" Width="35" Height="35" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10" Command="{Binding CloseOverlayCommand}" Background="Transparent" Foreground="White" BorderThickness="0" Cursor="Hand" ToolTip="Close" Style="{StaticResource HoverButtonStyle}"/>
        </Grid>
    </Border>

</UserControl>