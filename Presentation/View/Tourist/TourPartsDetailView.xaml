<UserControl x:Class="BookingApp.Presentation.View.Tourist.TourPartsDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E8E8E8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#95A5A6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="←" FontSize="16" FontWeight="Bold" Margin="0,0,8,0"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#7F8C8D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatusBadgeStyle" TargetType="Border">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

       
        <Style x:Key="DynamicStatusBadgeStyle" TargetType="Border" BasedOn="{StaticResource StatusBadgeStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="PENDING">
                    <Setter Property="Background" Value="#FEF5E7"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="ACCEPTED">
                    <Setter Property="Background" Value="#E8F5E8"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="INVALID">
                    <Setter Property="Background" Value="#FFEBEE"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="PENDING">
                    <Setter Property="Foreground" Value="#F39C12"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="ACCEPTED">
                    <Setter Property="Foreground" Value="#27AE60"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="INVALID">
                    <Setter Property="Foreground" Value="#E74C3C"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F4F6F7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        
        <Border Grid.Row="0" Background="White" BorderBrush="#E8E8E8" BorderThickness="0,0,0,1">
            <Grid Margin="40,25,40,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Nazad na listu" Style="{StaticResource BackButtonStyle}"
                        Command="{Binding BackToListCommand}" Margin="0,0,20,0"/>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="{Binding SelectedRequest.LocationText}" FontSize="24" FontWeight="Bold" 
                               Foreground="#2C3E50"/>
                    <TextBlock Text="{Binding SelectedRequest.DateRangeText}" FontSize="14" Foreground="#7F8C8D" 
                               Margin="0,5,0,0"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding SelectedRequest.TotalParts, StringFormat='Ukupno delova: {0}'}" 
                                   FontSize="12" Foreground="#7F8C8D" Margin="0,0,20,0"/>
                        <TextBlock Text="{Binding SelectedRequest.AcceptedParts, StringFormat='Prihvaćeno: {0}'}" 
                                   FontSize="12" Foreground="#27AE60" FontWeight="SemiBold"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="40,20">
            <ItemsControl ItemsSource="{Binding TourParts}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                
                                <Grid Grid.Row="0" Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock FontSize="18" FontWeight="Bold" Foreground="#2C3E50">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="Deo {0}: {1}">
                                                    <Binding Path="PartIndex"/>
                                                    <Binding Path="LocationText"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                        <TextBlock Text="{Binding DateRangeText}" FontSize="14" 
                                                   Foreground="#7F8C8D" Margin="0,5,0,0"/>
                                    </StackPanel>

                                    <Border Grid.Column="1" Style="{StaticResource DynamicStatusBadgeStyle}">
                                        <TextBlock Text="{Binding StatusText}" Style="{StaticResource StatusTextStyle}"/>
                                    </Border>
                                </Grid>

                                
                                <StackPanel Grid.Row="1" Margin="0,0,0,15">
                                    <TextBlock Text="Opis:" FontSize="12" FontWeight="SemiBold" 
                                               Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding Description}" FontSize="14" Foreground="#34495E" 
                                               TextWrapping="Wrap"/>
                                </StackPanel>

                                
                                <Grid Grid.Row="2" Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Jezik:" FontSize="12" FontWeight="SemiBold" 
                                                   Foreground="#7F8C8D" Margin="0,0,0,3"/>
                                        <TextBlock Text="{Binding Language}" FontSize="14" Foreground="#34495E"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="Broj učesnika:" FontSize="12" FontWeight="SemiBold" 
                                                   Foreground="#7F8C8D" Margin="0,0,0,3"/>
                                        <TextBlock Text="{Binding NumberOfPeople}" FontSize="14" Foreground="#34495E"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Visibility="{Binding IsAccepted, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="Vodič ID:" FontSize="12" FontWeight="SemiBold" 
                                                   Foreground="#7F8C8D" Margin="0,0,0,3"/>
                                        <TextBlock Text="{Binding AcceptedByGuideId}" FontSize="14" Foreground="#34495E"/>
                                    </StackPanel>
                                </Grid>

                                
                                <Border Grid.Row="3" Background="#E8F5E8" BorderBrush="#27AE60" 
                                        BorderThickness="1" Padding="15" CornerRadius="6"
                                        Visibility="{Binding IsAccepted, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Zakazani termin:" FontSize="12" FontWeight="SemiBold"
                                                       Foreground="#27AE60" Margin="0,0,0,5"/>
                                            <TextBlock Text="{Binding ScheduledDate, StringFormat='dd.MM.yyyy HH:mm'}" 
                                                       FontSize="14" FontWeight="Bold" Foreground="#2C3E50"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="Datum prihvatanja:" FontSize="12" FontWeight="SemiBold"
                                                       Foreground="#27AE60" Margin="0,0,0,5"/>
                                            <TextBlock Text="{Binding AcceptedDate, StringFormat='dd.MM.yyyy HH:mm'}" 
                                                       FontSize="14" Foreground="#2C3E50"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>