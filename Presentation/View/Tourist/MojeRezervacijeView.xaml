<UserControl x:Class="BookingApp.Presentation.View.Tourist.MojeRezervacijeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BookingApp.Presentation.ViewModel.Tourist">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#F0F0F0" ShadowDepth="2" BlurRadius="8" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2E86C1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#95A5A6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E74C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer Background="#F8F9FA" VerticalScrollBarVisibility="Auto">
        <Grid MaxWidth="1000" HorizontalAlignment="Center" Margin="20">
            <StackPanel>

                <!-- Header -->
                <TextBlock Text="MOJE REZERVACIJE" FontSize="24" FontWeight="Bold" 
                           Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,20,0,20"/>

                <!-- Filter and Refresh Controls -->
                <!-- ИСПРАВЉЕНО: Заменио HorizontalAlignment="SpaceBetween" са Grid -->
                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="Filtriranje:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <ComboBox ItemsSource="{Binding StatusFilterOptions}" 
                                      SelectedItem="{Binding SelectedStatusFilter}" 
                                      Width="120" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Button Grid.Column="1" Content="Osveži" Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding RefreshCommand}"/>
                    </Grid>
                </Border>

                <!-- Loading Indicator -->
                <Border Background="#E8F4FD" BorderBrush="#2E86C1" BorderThickness="1" 
                        CornerRadius="6" Padding="15" Margin="0,0,0,15"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="⏳ Učitavam rezervacije..." FontSize="14" Foreground="#2C3E50"/>
                    </StackPanel>
                </Border>

                <!-- Status Message -->
                <Border Background="#F8F9FA" BorderBrush="#BDC3C7" BorderThickness="1" 
                        CornerRadius="6" Padding="15" Margin="0,0,0,15"
                        Visibility="{Binding HasNoReservations, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="{Binding StatusMessage}" TextAlignment="Center" 
                               FontSize="14" Foreground="#7F8C8D"/>
                </Border>

                <!-- Reservations List -->
                <ItemsControl ItemsSource="{Binding FilteredReservations}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Reservation Details -->
                                    <StackPanel Grid.Column="0">
                                        <!-- Tour Name and Status -->
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                            <TextBlock Text="{Binding TourName}" FontSize="18" FontWeight="Bold" 
                                                       Foreground="#2C3E50" VerticalAlignment="Center"/>
                                            <Border Padding="8,4" Margin="15,0,0,0" CornerRadius="12">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="ACTIVE">
                                                                <Setter Property="Background" Value="#E8F5E8"/>
                                                                <Setter Property="BorderBrush" Value="#4CAF50"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="COMPLETED">
                                                                <Setter Property="Background" Value="#E3F2FD"/>
                                                                <Setter Property="BorderBrush" Value="#2196F3"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="CANCELLED">
                                                                <Setter Property="Background" Value="#FFEBEE"/>
                                                                <Setter Property="BorderBrush" Value="#F44336"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding StatusText}" FontSize="12" FontWeight="SemiBold">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="ACTIVE">
                                                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="COMPLETED">
                                                                    <Setter Property="Foreground" Value="#2196F3"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="CANCELLED">
                                                                    <Setter Property="Foreground" Value="#F44336"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                        </StackPanel>

                                        <!-- Reservation Info -->
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Text="Vodič:" Grid.Row="0" Grid.Column="0" FontWeight="SemiBold" 
                                                       Margin="0,0,10,5" Foreground="#34495E"/>
                                            <TextBlock Text="{Binding GuideName}" Grid.Row="0" Grid.Column="1" 
                                                       Margin="0,0,20,5" Foreground="#2C3E50"/>

                                            <TextBlock Text="Datum ture:" Grid.Row="0" Grid.Column="2" FontWeight="SemiBold" 
                                                       Margin="0,0,10,5" Foreground="#34495E"/>
                                            <TextBlock Text="{Binding TourDateFormatted}" Grid.Row="0" Grid.Column="3" 
                                                       Margin="0,0,0,5" Foreground="#2C3E50"/>

                                            <TextBlock Text="Broj učesnika:" Grid.Row="1" Grid.Column="0" FontWeight="SemiBold" 
                                                       Margin="0,0,10,5" Foreground="#34495E"/>
                                            <TextBlock Text="{Binding NumberOfGuests}" Grid.Row="1" Grid.Column="1" 
                                                       Margin="0,0,20,5" Foreground="#2C3E50"/>

                                            <TextBlock Text="Datum rezervacije:" Grid.Row="1" Grid.Column="2" FontWeight="SemiBold" 
                                                       Margin="0,0,10,5" Foreground="#34495E"/>
                                            <TextBlock Text="{Binding ReservationDate, StringFormat='dd.MM.yyyy'}" Grid.Row="1" Grid.Column="3" 
                                                       Margin="0,0,0,5" Foreground="#2C3E50"/>
                                        </Grid>
                                    </StackPanel>

                                    <!-- Action Buttons -->
                                    <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                                        <!-- Cancel Button -->
                                        <!-- ИСПРАВЉЕНО: Уклонио дуплиране Style атрибуте -->
                                        <Button Content="Otkaži"
                                                Command="{Binding DataContext.CancelReservationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource DangerButtonStyle}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="COMPLETED">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="CANCELLED">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <!-- Review Button -->
                                        <!-- ИСПРАВЉЕНО: Уклонио дуплиране Style атрибуте -->
                                        <Button Content="Oceni turu"
                                                Command="{Binding DataContext.ReviewTourCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="ACTIVE">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="CANCELLED">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>