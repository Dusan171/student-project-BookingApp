<UserControl x:Class="BookingApp.Presentation.View.Tourist.TourRequestListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Zahtevi za organizovanje tura" 
                       FontSize="24" FontWeight="Bold" 
                       Foreground="#2C3E50"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="+ Kreiraj novi zahtev" 
                    Command="{Binding CreateRequestCommand}"
                    Background="#2E86C1" Foreground="White"
                    Padding="20,10" FontSize="14" FontWeight="SemiBold"
                    BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <!-- Content -->
        <Border Grid.Row="2" Background="White" 
                BorderBrush="#E0E0E0" BorderThickness="1">
            <TextBlock Text="Lista zahteva ce biti ovde..." 
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontSize="16" Foreground="#7F8C8D"/>
        </Border>
    </Grid>
</UserControl>