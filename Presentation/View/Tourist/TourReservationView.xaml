<UserControl x:Class="BookingApp.Presentation.View.Tourist.TourReservationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BookingApp.Presentation.ViewModel.Tourist"
             xmlns:utils="clr-namespace:BookingApp.Utilities">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <utils:BooleanToTextConverter x:Key="BooleanToTextConverter"/>
        <utils:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <utils:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis"/>

        <!-- Validation Error Style -->
        <Style x:Key="ValidationErrorStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E74C3C"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>

        <!-- TextBox Style sa validacijom -->
        <Style x:Key="ValidationTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Padding="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#3498DB"/>
                                <Setter Property="Background" Value="#F8FDFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#85C1E9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Error TextBox Style -->
        <Style x:Key="ErrorTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource ValidationTextBoxStyle}">
            <Setter Property="BorderBrush" Value="#E74C3C"/>
            <Setter Property="Background" Value="#FDEEEE"/>
        </Style>

        <!-- Modern Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E8E8E8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" ShadowDepth="2" BlurRadius="10" Opacity="0.08"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1F5F8B"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDC3C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#BDC3C7"/>
                                <Setter Property="Background" Value="#F8F9FA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Add Guest Button Style -->
        <Style x:Key="AddGuestButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="+" FontSize="16" FontWeight="Bold" Margin="0,0,8,0"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#229954"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1E8449"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDC3C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Remove Guest Button Style -->
        <Style x:Key="RemoveGuestButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E74C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                            <TextBlock Text="×" FontSize="16" FontWeight="Bold" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C0392B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Guest Card Style -->
        <Style x:Key="GuestCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#E8E8E8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <!-- Error Overlay Style -->
        <Style x:Key="ErrorOverlayStyle" TargetType="Grid">
            <Setter Property="Background" Value="#88000000"/>
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>

        <Style x:Key="ErrorPanelStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E74C3C"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="MaxWidth" Value="500"/>
        </Style>

        <Style x:Key="SuccessPanelStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#27AE60"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="MaxWidth" Value="500"/>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="MainGrid" Background="#F4F6F7">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="50,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <!-- Leva kolona - Info o turi -->
                <StackPanel Grid.Column="0">
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="INFORMACIJE O TURI" FontSize="20" FontWeight="Bold" 
                                   Foreground="#2C3E50" Margin="0,0,0,20"/>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Naziv ture:" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding TourName}" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Lokacija:" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding LocationText}" FontSize="16" Foreground="#34495E"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Trajanje:" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding DurationText}" FontSize="16" Foreground="#34495E"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Jezik:" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding Language}" FontSize="16" Foreground="#34495E"/>
                            </StackPanel>

                            <StackPanel>
                                <TextBlock Text="Dostupnost:" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding AvailableSpotsText}" FontSize="16" FontWeight="Bold"
                                       Foreground="{Binding AvailableSpotsColor}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Alternativne ture levo -->
                    <Border Style="{StaticResource CardStyle}"
                        Visibility="{Binding HasAlternativeTours, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel>
                            <TextBlock Text="ALTERNATIVNE TURE" FontSize="18" FontWeight="Bold" 
                                   Foreground="#2C3E50" Margin="0,0,0,15"/>
                            <ItemsControl ItemsSource="{Binding AlternativeTours}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,10" 
                                            Padding="15" CornerRadius="8" Background="#F8F9FA">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                                                <TextBlock Text="{Binding AvailableSpotsText}" FontSize="12" 
                                                       Foreground="{Binding AvailableSpotsColor}" Margin="0,0,0,10"/>
                                                <Button Content="Odaberi" 
                                                    Command="{Binding DataContext.SelectAlternativeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource PrimaryButtonStyle}"
                                                    HorizontalAlignment="Stretch"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Desna kolona - Učesnici -->
                <StackPanel Grid.Column="2">
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <Grid Margin="0,0,0,20">
                                <TextBlock Text="UČESNICI TURE" FontSize="24" FontWeight="Bold" 
                                       Foreground="#2C3E50" VerticalAlignment="Center"/>
                                <Button Content="Dodaj učesnika" 
                                    Style="{StaticResource AddGuestButtonStyle}"
                                    Command="{Binding AddGuestCommand}"
                                    HorizontalAlignment="Right"/>
                            </Grid>

                            <ScrollViewer MaxHeight="600" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding GuestDetails}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource GuestCardStyle}">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <Grid Grid.Row="0" Margin="0,0,0,15">
                                                        <StackPanel Orientation="Horizontal">
                                                            <Ellipse Width="8" Height="8" Margin="0,0,10,0" VerticalAlignment="Center">
                                                                <Ellipse.Fill>
                                                                    <SolidColorBrush Color="{Binding IsMainContact, Converter={StaticResource BooleanToTextConverter}, ConverterParameter='#3498DB|#95A5A6'}"/>
                                                                </Ellipse.Fill>
                                                            </Ellipse>
                                                            <TextBlock FontWeight="Bold" FontSize="16" Foreground="#2C3E50" VerticalAlignment="Center">
                                                                <TextBlock.Text>
                                                                    <Binding Path="IsMainContact" Converter="{StaticResource BooleanToTextConverter}" ConverterParameter="Glavni kontakt|Učesnik"/>
                                                                </TextBlock.Text>
                                                            </TextBlock>
                                                        </StackPanel>

                                                        <Button Command="{Binding DataContext.RemoveGuestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                ToolTip="Ukloni učesnika"
                                                                Style="{StaticResource RemoveGuestButtonStyle}"
                                                                HorizontalAlignment="Right">
                                                            <Button.Visibility>
                                                                <Binding Path="IsMainContact">
                                                                    <Binding.Converter>
                                                                        <utils:InverseBoolToVisibilityConverter/>
                                                                    </Binding.Converter>
                                                                </Binding>
                                                            </Button.Visibility>
                                                        </Button>
                                                    </Grid>

                                                    <Grid Grid.Row="1">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="15"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="15"/>
                                                            <ColumnDefinition Width="100"/>
                                                        </Grid.ColumnDefinitions>

                                                        <StackPanel Grid.Column="0">
                                                            <TextBlock Text="Ime *" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#34495E"/>
                                                            <TextBox x:Name="FirstNameTextBox"
                                                                     Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" 
                                                                     Style="{StaticResource ValidationTextBoxStyle}"
                                                                     LostFocus="ValidateGuestField"/>
                                                            <TextBlock x:Name="FirstNameError" 
                                                                       Style="{StaticResource ValidationErrorStyle}"/>
                                                        </StackPanel>

                                                        <StackPanel Grid.Column="2">
                                                            <TextBlock Text="Prezime *" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#34495E"/>
                                                            <TextBox x:Name="LastNameTextBox"
                                                                     Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" 
                                                                     Style="{StaticResource ValidationTextBoxStyle}"
                                                                     LostFocus="ValidateGuestField"/>
                                                            <TextBlock x:Name="LastNameError" 
                                                                       Style="{StaticResource ValidationErrorStyle}"/>
                                                        </StackPanel>

                                                        <StackPanel Grid.Column="4">
                                                            <TextBlock Text="Godine *" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#34495E"/>
                                                            <TextBox x:Name="AgeTextBox"
                                                                     Text="{Binding Age, UpdateSourceTrigger=PropertyChanged}" 
                                                                     Style="{StaticResource ValidationTextBoxStyle}"
                                                                     TextAlignment="Center"
                                                                     LostFocus="ValidateGuestField"/>
                                                            <TextBlock x:Name="AgeError" 
                                                                       Style="{StaticResource ValidationErrorStyle}"/>
                                                        </StackPanel>
                                                    </Grid>

                                                    <StackPanel Grid.Row="2" Margin="0,15,0,0"
                                                            Visibility="{Binding IsMainContact, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <TextBlock Text="Email *" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#34495E"/>
                                                        <TextBox x:Name="EmailTextBox"
                                                                 Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                                                                 Style="{StaticResource ValidationTextBoxStyle}"
                                                                 LostFocus="ValidateGuestField"/>
                                                        <TextBlock x:Name="EmailError" 
                                                                   Style="{StaticResource ValidationErrorStyle}"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>

                            <!-- Dugmići -->
                            <Grid Margin="0,20,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="15"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Button Grid.Column="0" Content="Otkaži" 
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Click="CancelButton_Click"/>

                                <Button Grid.Column="2" 
                                        Content="POTVRDI REZERVACIJU"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Click="ConfirmReservation_Click"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <!-- Success Overlay -->
        <Grid x:Name="SuccessOverlay" Style="{StaticResource ErrorOverlayStyle}">
            <Border Style="{StaticResource SuccessPanelStyle}" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="✓" FontSize="24" FontWeight="Bold" Foreground="#27AE60" Margin="0,0,10,0"/>
                        <TextBlock Text="REZERVACIJA USPEŠNA" FontWeight="Bold" FontSize="16" Foreground="#27AE60"/>
                    </StackPanel>

                    <TextBlock x:Name="SuccessMessage" 
                               Text="Tura je uspešno rezervisana!" 
                               FontSize="14" Foreground="#7F8C8D" 
                               Margin="0,0,0,20" TextWrapping="Wrap"/>

                    <Button Content="ZATVORI" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Background="#27AE60"
                            Click="CloseSuccessOverlay"
                            HorizontalAlignment="Right"
                            MinWidth="100"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Error Overlay -->
        <Grid x:Name="ErrorOverlay" Style="{StaticResource ErrorOverlayStyle}">
            <Border Style="{StaticResource ErrorPanelStyle}" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="!" FontSize="24" FontWeight="Bold" Foreground="#E74C3C" Margin="0,0,10,0"/>
                        <TextBlock Text="GREŠKA PRI REZERVACIJI" FontWeight="Bold" FontSize="16" Foreground="#E74C3C"/>
                    </StackPanel>

                    <TextBlock x:Name="ErrorMessage" 
                               Text="Molimo popunite sva obavezna polja:" 
                               FontSize="14" Foreground="#7F8C8D" 
                               Margin="0,0,0,15" TextWrapping="Wrap"/>

                    <Button Content="ZATVORI" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="CloseErrorOverlay"
                            HorizontalAlignment="Right"
                            MinWidth="100"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>