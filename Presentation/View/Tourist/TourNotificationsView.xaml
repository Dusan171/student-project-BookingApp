<UserControl x:Class="BookingApp.Presentation.View.Tourist.TourNotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BookingApp.Presentation.ViewModel.Tourist">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E8E8E8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" ShadowDepth="2" BlurRadius="8" Opacity="0.06"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NotificationCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#F8FBFE"/>
            <Setter Property="BorderBrush" Value="#3498DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#3498DB"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderBrush" Value="#3498DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3498DB"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

       
        <Style x:Key="GrayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#95A5A6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#7F8C8D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F4F6F7">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="40,20">

                
                <StackPanel Margin="0,0,0,30">
                    <TextBlock Text="OBAVEŠTENJA O NOVIM TURAMA" FontSize="26" FontWeight="Bold" 
                               Foreground="#2C3E50" Margin="0,0,0,10"/>
                    <TextBlock Text="Najnovije obaveštenje o kreiranim turama koje odgovaraju vašim zahtevima" FontSize="14" 
                               Foreground="#7F8C8D"/>
                </StackPanel>

                
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Najnovije obaveštenje" FontSize="18" FontWeight="Bold" 
                                   Foreground="#2C3E50" Margin="0,0,0,15"/>

                        <Border Background="#E8F5E8" BorderBrush="#27AE60" BorderThickness="1" 
                                CornerRadius="8" Padding="15" Margin="0,0,0,15"
                                Visibility="{Binding HasLatestNotification, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Viewbox Width="24" Height="24" Margin="0,0,15,0" Grid.Column="0">
                                    <Path Data="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z" 
                                          Fill="#27AE60"/>
                                </Viewbox>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding LatestNotification.Title}" FontSize="14" FontWeight="SemiBold" 
                                               Foreground="#27AE60" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding LatestNotification.Message}" FontSize="12" 
                                               Foreground="#2C3E50" TextWrapping="Wrap"/>
                                </StackPanel>

                                <Button Grid.Column="2" Content="Pogledaj detalje" 
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Command="{Binding ViewTourDetailsCommand}"
                                        CommandParameter="{Binding LatestNotification}"
                                        VerticalAlignment="Center"/>
                            </Grid>
                        </Border>

                       
                        <Border Background="#FFF3CD" BorderBrush="#F39C12" BorderThickness="1" 
                                CornerRadius="8" Padding="20"
                                Visibility="Collapsed">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="Nema novih obaveštenja" FontSize="16" FontWeight="SemiBold" 
                                           Foreground="#856404" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <TextBlock Text="Kada vodići kreiraju nove ture na osnovu vaših zahteva, ovde ćete biti obavešteni" 
                                           FontSize="12" Foreground="#856404" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>




                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Istorija obaveštenja" FontSize="18" FontWeight="Bold" 
                                   Foreground="#2C3E50" Margin="0,0,0,20"/>

                        <ItemsControl ItemsSource="{Binding NotificationHistory}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource NotificationCardStyle}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Viewbox Width="20" Height="20" Margin="0,0,15,0" Grid.Column="0">
                                                <Path Data="M19 3H5c-1.1 0-1.99.9-1.99 2L3 19c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-5 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z" 
                                                      Fill="#3498DB"/>
                                            </Viewbox>

                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" 
                                                           Foreground="#2C3E50" Margin="0,0,0,5"/>
                                                <TextBlock Text="{Binding Message}" FontSize="12" 
                                                           Foreground="#34495E" TextWrapping="Wrap" Margin="0,0,0,5"/>
                                                <TextBlock Text="{Binding CreatedAt, StringFormat='dd.MM.yyyy HH:mm'}" 
                                                           FontSize="10" Foreground="#7F8C8D"/>
                                            </StackPanel>

                                            <Button Grid.Column="2" Content="Detalji" 
                                                    Command="{Binding DataContext.ViewTourDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource SecondaryButtonStyle}"
                                                    VerticalAlignment="Top"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        
                        <Border Background="#F8F9FA" BorderBrush="#E8E8E8" BorderThickness="1" 
                                CornerRadius="8" Padding="30" 
                                Visibility="Collapsed">
                            <!--Visibility="{Binding HasLatestNotification, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}"-->
                            <StackPanel HorizontalAlignment="Center">
                                <Viewbox Width="48" Height="48" Margin="0,0,0,15">
                                    <Path Data="M19 3H5c-1.1 0-1.99.9-1.99 2L3 19c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-5 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z" 
                                          Fill="#BDC3C7"/>
                                </Viewbox>
                                <TextBlock Text="Nema obaveštenja u istoriji" FontSize="16" FontWeight="SemiBold" 
                                           Foreground="#7F8C8D" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>