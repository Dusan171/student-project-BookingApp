<Window x:Class="BookingApp.Presentation.View.Guest.SuggestedDatesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        xmlns:loc="clr-namespace:BookingApp.Utilities"
        
        mc:Ignorable="d"
        Title="Alternative Dates" Height="450" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource PrimaryBackgroundColor}">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock FontSize="28" FontWeight="Bold" Foreground="{DynamicResource DangerColor}"
                       Text="{loc:Translate SuggestedDates_Header}"/>
            <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" FontSize="16" Foreground="{DynamicResource SecondaryTextColor}"
                       Text="{loc:Translate SuggestedDates_Description}"/>
        </StackPanel>

        <Border Grid.Row="1" Background="{DynamicResource SecondaryBackgroundColor}" CornerRadius="5" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
            <ListView ItemsSource="{Binding SuggestedDateRanges}"
                      SelectedItem="{Binding SelectedDateRange}"
                      BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto"
                      Background="Transparent">

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="15,12"/>
                        <Setter Property="FontSize" Value="18"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextColor}"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentPrimaryColor}"/>
                                <Setter Property="Foreground" Value="{DynamicResource AccentForegroundColor}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MenuButtonHoverColor}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="{loc:Translate SuggestedDates_ReserveButton}" Style="{StaticResource PrimaryButton}" IsDefault="True" Command="{Binding ConfirmCommand}"/>
            <Button Content="{loc:Translate Common_Cancel}" Style="{StaticResource SecondaryButton}" IsCancel="True" Command="{Binding CancelCommand}" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>