<Window x:Class="BookingApp.Presentation.View.Guest.GuestMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  
        xmlns:vm="clr-namespace:BookingApp.Presentation.ViewModel.Guest"
        xmlns:view="clr-namespace:BookingApp.Presentation.View.Guest"
        xmlns:loc="clr-namespace:BookingApp.Utilities"
        mc:Ignorable="d"
        
        Title="{loc:Translate MainWindow_Title}" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource PrimaryBackgroundColor}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:AccommodationLookupViewModel}">
            <view:AccommodationLookupPage/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MyReservationsViewModel}">
            <view:MyReservationsPage/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AnywhereAnytimeViewModel}">
            <view:AnywhereAnytimePage/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ForumListViewModel}">
            <view:ForumListPage/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ForumViewViewModel}">
            <view:ForumViewPage/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <view:SettingsPage/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AccommodationDetailsViewModel}">
            <view:AccommodationDetailsPage/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource SecondaryBackgroundColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,1,0">
            <Grid Width="220">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="BookingApp" FontSize="24" FontWeight="Bold" Margin="20" 
                           Foreground="{DynamicResource AccentPrimaryColor}"/>

                <StackPanel Grid.Row="1" VerticalAlignment="Top">
                    <RadioButton Content="{loc:Translate Menu_SearchAccommodations}" Command="{Binding ShowAccommodationsCommand}" Style="{StaticResource MenuButtonStyle}" IsChecked="True"/>
                    <RadioButton Content="{loc:Translate Menu_AnywhereAnytime}" Command="{Binding ShowAnywhereAnytimeCommand}" Style="{StaticResource MenuButtonStyle}"/>
                    <RadioButton Content="{loc:Translate Menu_MyReservations}" Command="{Binding ShowMyReservationsCommand}" Style="{StaticResource MenuButtonStyle}"/>
                    <RadioButton Content="{loc:Translate Menu_Forums}" Command="{Binding ShowForumsCommand}" Style="{StaticResource MenuButtonStyle}"/>
                </StackPanel>

                <StackPanel Grid.Row="2" VerticalAlignment="Bottom">
                    <Separator Style="{StaticResource GuestSeparatorStyle}" Margin="20,10"/>
                    <RadioButton Content="{loc:Translate Menu_Settings}" Command="{Binding ShowSettingsCommand}" Style="{StaticResource MenuButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}" Margin="20"/>

    </Grid>
</Window>