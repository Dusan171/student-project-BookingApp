<Window x:Class="BookingApp.Presentation.View.Guest.GuestMainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Guest Menu" 
        Height="250" Width="350"
        WindowStartupLocation="CenterScreen"
        SizeToContent="WidthAndHeight" ResizeMode="NoResize">

    <Window.Resources>
        <!-- Definišemo jedan stil koji će se primeniti na sva dugmad unutar prozora -->
        <Style TargetType="Button">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <!-- Centralni StackPanel za dugmad -->
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <Button Content="View Accommodations" Click="ViewAccommodations_Click"/>
        <Button Content="My Reservations" Click="MyReservations_Click"/>
    </StackPanel>
</Window>