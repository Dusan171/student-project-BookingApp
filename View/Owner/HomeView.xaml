<UserControl x:Class="BookingApp.View.Owner.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookingApp.View.Owner"
             mc:Ignorable="d" d:DesignHeight="720" d:DesignWidth="1200">
    <Grid Margin="0,200,0,100">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.7*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEVA STRANA -->
        <Border Grid.Column="0" Background="#F2F2F2" CornerRadius="30" Margin="10,10,10,76" Padding="20">
            <StackPanel HorizontalAlignment="Center" Width="487">
                <!-- Avatar -->
                <Ellipse Width="100" Height="100" Stroke="Gray" StrokeThickness="2" Margin="0,0,0,10">
                    <Ellipse.Fill>
                        <SolidColorBrush Color="LightCyan"/>
                    </Ellipse.Fill>
                </Ellipse>

                <!-- Dinamičko korisničko ime -->
                <TextBlock x:Name="UsernameTextBlock" FontWeight="Bold" FontSize="16" Margin="0,0,0,20" TextAlignment="Center"/>

                <!-- Navigacija -->
                <Button x:Name="NotificationsButton" Content="📢 Notifications" Margin="0,5" Click="Notifications_Click"/>
                <Button Content="👥 Community" Margin="0,5" Click="Community_Click"/>
                <Button Content="📚 My Library" Margin="0,5" Click="Library_Click"/>
            </StackPanel>
        </Border>

        <!-- DESNA STRANA -->
        <ContentControl x:Name="MainContent" Grid.Column="1" Margin="20"/>


        <Border x:Name="ToastPopup"
        Grid.Column="1"
        Background="LightGray"
        BorderBrush="Black"
        BorderThickness="1"
        Padding="10"
        CornerRadius="20"
        Width="250"
        Height="70"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Margin="20"
        Visibility="Collapsed"
        Opacity="1">
            <TextBlock x:Name="ToastMessage" Text="You have unrated guests!" TextAlignment="Center" FontWeight="Bold" Foreground="Black"/>
        </Border>

    </Grid>
</UserControl>
