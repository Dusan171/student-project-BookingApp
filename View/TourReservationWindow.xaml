<Window x:Class="BookingApp.View.TourReservationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View"
        mc:Ignorable="d"
        Title="Rezervacija ture" Height="500" Width="700" WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Info o turi -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="{Binding Tour.Name}" FontSize="16" FontWeight="Bold"/>
            <TextBlock Text="{Binding Tour.Description}" TextWrapping="Wrap"/>
            <TextBlock>
                <Run Text="{Binding Tour.Location.City}"/>
                <Run Text=", "/>
                <Run Text="{Binding Tour.Location.Country}"/>
            </TextBlock>
            <TextBlock>
                <Run Text="Maksimalan broj turista: "/>
                <Run Text="{Binding Tour.MaxTourists}"/>
            </TextBlock>
            <TextBlock>
                <Run Text="Rezervisano mesta: "/>
                <Run Text="{Binding Tour.ReservedSpots}"/>
            </TextBlock>
        </StackPanel>

        <!-- Gosti -->
        <GroupBox Header="Gosti" Grid.Row="1">
            <StackPanel>
                <ItemsControl Name="GuestsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <TextBox Width="120" Text="{Binding FirstName}" />
                                <TextBox Width="120" Text="{Binding LastName}" Margin="10,0,0,0"/>
                                <TextBox Width="50" Text="{Binding Age}" Margin="10,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button Content="Dodaj gosta" Margin="0,10,0,0" Width="100" Click="AddGuest_Click"/>
            </StackPanel>
        </GroupBox>

        <!-- Dugmad -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Potvrdi rezervaciju" Width="150" Margin="0,0,10,0" Click="Confirm_Click"/>
            <Button Content="Otkaži" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
